define(['angular', 'lodash'], function(angular, _){

	'use strict';

	angular.module('Controllers')
		.controller('UsersCtrl', [
			'$scope',
			function($scope){

				//show all the users

			}
		])
		.controller('UserCtrl', [
			'$scope',
			function($scope){

				//show a particular user when there is a userLink (this is where the author link goes)!

			}
		]);

});